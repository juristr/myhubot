#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../hubot/bin/hubot" "$@"
  ret=$?
else 
  coffee  "$basedir/../hubot/bin/hubot" "$@"
  ret=$?
fi
exit $ret
